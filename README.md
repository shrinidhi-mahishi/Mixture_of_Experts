# Mixture_of_Experts

We we'll create an MoE model with three experts, each trained on a different subset of the data. The gating network will decide how much each expert should contribute to the final prediction.
